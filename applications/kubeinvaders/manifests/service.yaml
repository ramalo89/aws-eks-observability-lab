apiVersion: v1
kind: Service
metadata:
  name: kubeinvaders
  namespace: kubeinvaders
  labels:
    app.kubernetes.io/name: kubeinvaders
spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: kubeinvaders

  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
